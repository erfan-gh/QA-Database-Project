extends layout

block content
	.container
		each topic in topics
				.panel.panel-primary
					.panel-heading=topic.first_name + ',' + topic.last_name 
					.panel-body=topic.title
					input.btn.btn-primary(type='button', value='view more', onclick="location.href = '/answer/?id=5';")
